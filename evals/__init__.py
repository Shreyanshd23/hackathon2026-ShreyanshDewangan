"""Offline evaluation harness: golden set, deterministic metrics, CI gate.

Import from submodules directly (e.g. `from evals.runner import build_scorecard`)
to avoid importing the pipeline at package-import time.
"""
